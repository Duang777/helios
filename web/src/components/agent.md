# Components Agent Guide

Components should be focused and composable. Avoid oversized cards, nested cards, and decorative gradients.

Use tables/lists/panels for operational data. Repeated workflow nodes may use small cards with radius <= 8px.

