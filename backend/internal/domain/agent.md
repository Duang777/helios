# Domain Agent Guide

Domain types are the public language of the backend and frontend. Changes here ripple outward.

Use JSON field names in camelCase. Prefer additive optional fields over renaming or changing existing fields.

