# Web Claude Guide

Run checks before handoff:

```bash
npm run build
```

When a browser is available, verify the page at desktop and mobile widths. Keep API types in `src/api/types.ts` aligned with backend JSON contracts.

