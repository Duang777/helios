# Runtime Agent Guide

The runtime executes workflow DAG nodes, records node run status, emits evidence and artifacts, and produces auditable run history.

Keep node execution deterministic unless an explicit executor adapter is injected.

