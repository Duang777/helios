# Compiler Agent Guide

The compiler converts business goals into workflow templates. The MVP compiler is deterministic and template-based so tests and demos are stable.

Future LLM compiler implementations should preserve the same output contract and validate generated workflow JSON before saving.

